export interface Options {
  pullMedia?: boolean;
  pullContent?: boolean;
  processMedia?: string;
  pageParser?: boolean;
  callFunctions?: boolean;
  apiKey?: string;
  apiSecret?: string;
  apiOrigin?: string;
}
