export interface Options {
  pullMedia?: boolean;
  pullContent?: boolean;
  processMedia?: boolean;
  media?: string;
  configMedia?: string;
  pageParser?: boolean;
  callFunctions?: boolean;
  apiKey?: string;
  apiSecret?: string;
  apiOrigin?: string;
}
